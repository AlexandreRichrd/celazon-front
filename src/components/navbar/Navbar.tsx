import './styles.scss'

import SearchBar from '@components/inputs/search-bar/SearchBar';

const NavBar = () => {
    return(
    <nav id="navbar">
        <SearchBar />
    </nav>
    )
}
 export default NavBar;